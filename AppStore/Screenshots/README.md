# App Store screenshots

`iPhone-6.5` contains historical 1242×2688 submission assets. They predate the
current themes and interface; recapture the intended release build before using
them for a new submission. Use `bin/screens` for source-verified QA captures.
